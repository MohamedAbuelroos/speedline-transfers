        // {/* ROUTE */}
        // <Typography sx={{ fontWeight: 600, mb: 1 }}>Route</Typography>

        // <Typography sx={{ mb: 2 }}>
        //   {data.from} → {data.to}
        // </Typography>

        // {/* DATE */}
        // <Typography>Date: {formatDate(data.date)}</Typography>

        // <Typography sx={{ mb: 2 }}>Time: {formatTime(data.time)}</Typography>

        // <Divider sx={{ my: 3 }} />

        // {/* VEHICLE */}
        // <Typography sx={{ fontWeight: 600, mb: 1 }}>Vehicle</Typography>

        // <Typography sx={{ mb: 2 }}>
        //   {data.car?.name} ({data.car?.category})
        // </Typography>

        // {/* PASSENGERS */}
        // <Typography>Passengers: {totalPassengers}</Typography>

        // <Divider sx={{ my: 3 }} />

        // {/* CUSTOMER */}
        // <Typography sx={{ fontWeight: 600, mb: 1 }}>
        //   Passenger Details
        // </Typography>

        // <Typography>Name: {data.name}</Typography>
        // <Typography>Phone: {data.phone}</Typography>

        // {data.notes && (
        //   <Typography sx={{ mt: 1 }}>Notes: {data.notes}</Typography>
        // )}